#!/bin/bash
# Run this shell in "terraform init" directory before executing "terraform apply".

echo "If you want to connect db instance, execute 'mysql -u root --password <db_password> --host <rds_endpoint>' on EC2 instance"

#!/bin/bash
# Run this shell in "terraform init" directory before executing "terraform apply".

echo "If you want to connect db instance, execute 'mysql -u admin -p <db_password> -h <rds_endpoint>' on EC2 instance"
